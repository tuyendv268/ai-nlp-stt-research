# ai-nlp-stt-research

```
- Open the 'src/pipeline' folder to run pipeline:
    + Perform forced alignment, and verify and clean the speech-to-text data.
    + Create speech-to-text data using voice cloning and text-to-speech.

- Open the 'scripts' folder to find scripts about training and testing model:
    + Check run.sh file
```